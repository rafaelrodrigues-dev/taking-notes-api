services:
  db:
    image: postgres:17
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - ./dotenv_files/.env
    networks:
      - note-taking-network
  web:
    image: rafaelrodriguess/note-taking-api
    ports:
      - "8000:8000"
    depends_on:
      - db
    env_file:
      - ./dotenv_files/.env
    networks:
      - note-taking-network

volumes:
  postgres_data:

networks:
  note-taking-network: